version: "3.8"

services:
  agri-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agri-solutions-bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./agri_bot.db:/app/agri_bot.db
